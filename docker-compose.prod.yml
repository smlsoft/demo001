version: "3.8"

services:
  thaiclaw:
    build: .
    container_name: demo001-web
    restart: unless-stopped
    ports:
      - "3100:3000"
    extra_hosts:
      - "aigw-local.hiclaw.io:host-gateway"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
