version: "3.8"

services:
  thaiclaw:
    build: .
    container_name: demo001-web
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3100
      - HOSTNAME=0.0.0.0
