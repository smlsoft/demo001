version: "3.8"

services:
  thaiclaw:
    build: .
    container_name: demo001-web
    restart: unless-stopped
    network_mode: host
    extra_hosts:
      - "aigw-local.hiclaw.io:127.0.0.1"
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
      - PORT=3100
      - HOSTNAME=0.0.0.0
